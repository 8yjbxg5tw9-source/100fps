# Core runtime dependencies for the 720p -> 8K @ 1000 FPS pipeline.
#
# NOTE on PyTorch + CUDA:
#   The default `pip install torch torchvision` wheels work on CPU and, for
#   many platforms, already include CUDA support. If Step 1 reports "CPU mode"
#   on a machine that HAS an NVIDIA GPU, install the CUDA build matching your
#   driver from https://pytorch.org/get-started/locally/ , e.g.:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   Step 1 (pipeline/step01_environment.py) can auto-install the CPU/default
#   wheels for any missing package, but it deliberately does NOT guess which
#   CUDA build you need -- that choice is left to requirements / manual setup.

torch>=2.0
torchvision>=0.15
opencv-python>=4.8
numpy>=1.24
Pillow>=10.0
tqdm>=4.66

# Dev / test only:
# pytest>=7.0
