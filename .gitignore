# Local working data (raw/interpolated/upscaled frames are huge -- never commit)
workspace/
# Downloaded RIFE weights (40-170 MB .pkl files -- never commit)
weights/
*.mp4
*.mkv
*.mov
*.avi
*.webm

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Test / OS noise
.pytest_cache/
.DS_Store

# Step 10: packaging artefacts (huge, reproducible — never commit)
packaging/vendor/
packaging/dist/
packaging/build/
